void* malloc (size_t);
void* calloc (size_t, size_t);
void* realloc (void*, size_t);
void free (void*);

void* memset (void*, int, size_t);

void srand (int);
int rand ();

int atoi (const char*);

int system (const char*);